############################# Filebeat #####################################
filebeat:
  prospectors:
    -
      paths:
        - {{ app_dir }}/server.log
      fields:
        logzio_codec: plain
        token: {{ logzio_token }}
        type: Logback
      multiline:
        # 2016-02-17 09:32:25.853 XXX
        pattern: ^20[0-9][0-9]-[0-9][0-9]-[0-9][0-9] [0-9][0-9]:[0-9][0-9]:[0-9][0-9]\.[0-9][0-9][0-9]
        negate: true
        match: after
      fields_under_root: true
      encoding: utf-8
      ignore_older: 3h
  registry_file: /var/lib/filebeat/registry
############################# Output ##########################################
output:
  logstash:
    hosts: ["listener.logz.io:5015"]

#########  The below configuration is used for Filebeat 1.3 or lower
    tls:
      certificate_authorities: ['/etc/pki/tls/certs/COMODORSADomainValidationSecureServerCA.crt']

########  The below configuration is used for Filebeat 5.0 or higher
    ssl:
      certificate_authorities: ['/etc/pki/tls/certs/COMODORSADomainValidationSecureServerCA.crt']
